{
  "train": {
    "epochs": 80,
    "batch_size": 64,
    "device": "cuda:0",
    "name": "real_data-run1",
    "optim": {
      "name": "adam",
      "lr": 0.0004,
      "lr_decay_steps": 35, 
      "gamma": 0.5
    },
    "model": {
      "model": "mst",
      "dim": 25
    },
    "evaluation": {
      "batch_size": 64
    }
  }
}