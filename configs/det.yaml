{
  "train": {
    "epochs": 80,
    "batch_size": 8,
    "device": "cuda:1",
    "name": "new_arrangement",
    "optim": {
      "name": "adam",
      "lr": 0.0002,
      "lr_decay_steps": 15, 
      "gamma": 0.5
    },
    "model": {
      "model": "det",
      "dim": 32
    }
  }
}