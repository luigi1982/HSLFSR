{
  "train": {
    "epochs": 50,
    "batch_size": 64,
    "device": "cuda:1",
    "name": "dim32",
    "optim": {
      "name": "adam",
      "lr": 0.0002,
      "lr_decay_steps": 15, 
      "gamma": 0.5
    },
    "model": {
      "model": "epit",
      "dim": 32
    }
  }
}