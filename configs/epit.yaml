{
  "train": {
    "epochs": 80,
    "batch_size": 16,
    "device": "cuda:0",
    "name": "3-Blocks",
    "optim": {
      "name": "adam",
      "lr": 0.0002,
      "lr_decay_steps": 15, 
      "gamma": 0.5
    },
    "model": {
      "model": "epit",
      "dim": 64
    }
  }
}